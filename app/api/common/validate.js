const { check, body, cookie, header, param, query, oneOf, matchedData } = require('express-validator')

module.exports = Object.freeze({
  check,
  body,
  cookie,
  header,
  param,
  query,
  oneOf,
  matchedData
})
